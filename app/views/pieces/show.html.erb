<p id="notice"><%= notice %></p>

<p>
  <h1><%= @piece.name %></h1>
</p>
<p>
  <% @piece.images.each do |image| %>
    <div>
      <%= image_tag image %>
    </div>
  <% end %>
</p>

<%= link_to 'Edit', edit_piece_path(@piece) %>
